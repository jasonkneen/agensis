// Moved to @agensis/ui (packages/ui/src/components/ui/radio-group.tsx).
// Temporary re-export shim so existing '@/components/ui/radio-group' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/radio-group';
