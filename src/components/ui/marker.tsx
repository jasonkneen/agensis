// Moved to @agensis/ui (packages/ui/src/components/ui/marker.tsx).
// Temporary re-export shim so existing '@/components/ui/marker' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/marker';
