// Moved to @agensis/ui (packages/ui/src/components/ui/calendar.tsx).
// Temporary re-export shim so existing '@/components/ui/calendar' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/calendar';
