// Moved to @agensis/ui (packages/ui/src/components/ui/toggle.tsx).
// Temporary re-export shim so existing '@/components/ui/toggle' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/toggle';
