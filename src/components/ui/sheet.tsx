// Moved to @agensis/ui (packages/ui/src/components/ui/sheet.tsx).
// Temporary re-export shim so existing '@/components/ui/sheet' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/sheet';
