// Moved to @agensis/ui (packages/ui/src/components/ui/scroll-area.tsx).
// Temporary re-export shim so existing '@/components/ui/scroll-area' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/scroll-area';
