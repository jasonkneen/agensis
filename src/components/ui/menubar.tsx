// Moved to @agensis/ui (packages/ui/src/components/ui/menubar.tsx).
// Temporary re-export shim so existing '@/components/ui/menubar' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/menubar';
