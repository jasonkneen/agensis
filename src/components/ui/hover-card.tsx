// Moved to @agensis/ui (packages/ui/src/components/ui/hover-card.tsx).
// Temporary re-export shim so existing '@/components/ui/hover-card' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/hover-card';
