// Moved to @agensis/ui (packages/ui/src/components/ui/native-select.tsx).
// Temporary re-export shim so existing '@/components/ui/native-select' imports keep
// working during the extraction; removed once call sites are rewritten.
export * from '@agensis/ui/components/native-select';
