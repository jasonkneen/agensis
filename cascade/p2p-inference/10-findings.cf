HUMAN: agensis already owns 80% of the mesh; port the browser node + add a gateway, not the burrow.
FIND F1 sev=H tag=gap at=agent/agensis-cli/src/agensis.mjs:410-452#"function buildAgentCommand" what="agents get a model ONLY via --model on a spawned CLI; no base-url/provider plumbing, so nothing can point a job at a custom inference endpoint" fix=add-base-url-injection ref=agent/agensis-cli/src/agensis.mjs:419,447 dep=-
FIND F2 sev=H tag=protocol-mismatch at=../almostnode_manager/src/forge/mesh-types.ts:109-126#"interface FulfillRequestPayload" what="mesh fulfill is single-shot prompt/systemPrompt/maxTokens, no streaming/tool_use/multi-turn; a coding CLI needs full Messages API" fix=scope-to-openai-chat+cheap-tasks-not-frontier-loops ref=../almostnode_manager/src/forge/mesh-client.ts:365-409 dep=-
FIND F3 sev=M tag=reuse at=server/mcp.cjs:749#"name: 'register_agent'" what="register/approve/token machinery (kinds workspace|user|invite, popup/auto-approve, presence) already exists; a provider node is just a new registration KIND advertising ModelInfo[]" fix=add-provider-kind ref=server/mcp.cjs:783,810 dep=F5
FIND F4 sev=M tag=reuse at=server/index.cjs:2781-2813#"function capabilitiesDriftNudges" what="capability-drift heartbeat + agent_connections.capabilities jsonb already carry & refresh a live daemon-owned advertisement — ideal channel for live ModelInfo[]/tps/busy" fix=carry-models-in-capabilities ref=server/index.cjs:673,2880 dep=-
FIND F5 sev=M tag=schema at=server/index.cjs:673#"capabilities jsonb DEFAULT" what="no provider/model registry table; need providers + provider_models (or a models[] block inside capabilities) plus a live-node routing set with TTL like the existing mcp presence" fix=add-provider-registry ref=server/index.cjs:846#"CREATE TABLE IF NOT EXISTS agent_registrations" dep=-
FIND F6 sev=M tag=port at=../almostnode_manager/src/forge/mesh-client.ts:81-460#"class MeshClient" what="the whole browser node (connect+token, announceModels, heartbeat, fulfill loop, reconnect) is directly portable; retarget wsUrl at agensis WS, drop crumb economy" fix=port-as-agensis-provider-client ref=../almostnode_manager/src/forge/mesh-client.ts:118-184,365-409 dep=F2
FIND F7 sev=L tag=port at=../almostnode_manager/relay/server.mjs:54-101#"wss.on('connection'" what="dumb token-gated 2-slot WS byte-bridge = the 'proxy my existing local gateway/models out without opening a port' primitive; fold into agensis WS server, don't run a separate relay" fix=inline-relay-into-agensis-ws ref=../almostnode_manager/relay/server.mjs:18-31 dep=-
FIND F8 sev=L tag=asset at=../almostnode_manager/index.html:1944-1955#"const WEBLLM_CDN" what="real WebGPU inference via @mlc-ai/web-llm@0.2.79 (CDN); transformers.js path in examples/openclaw-webgpu-demo.html:502-507 — both portable as the browser provider container" fix=ship-provider-page-asset ref=../almostnode_manager/examples/openclaw-webgpu-demo.html:780-783 dep=F6
RESOLVED Q1 -> r2: cross-tenant WITH real money on SOLANA. NOT internal-only, NOT the crumb economy. Humans+agents get a prepaid wallet/balance; gateway meters+debits off-chain; on-chain deposit + net settlement at edges. See 20-plan.cf P5 + OPEN QW1-4. Also drops the 'provider' abstraction: an agent IS a provider (advertises its models via capability channel F4) — supersedes P1/P2/P9.
OPEN Q2 what="two consumer surfaces — OpenAI-compatible /v1/chat/completions for external clients AND an internal model id (mesh/<model>) a local agensis agent can be pointed at; ship both or gateway-first?"
DONE stage=F find=8 open=2 tok~=560
