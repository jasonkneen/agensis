Full code review — whole codebase, architecture-level.

Scope: the entire current state of the agensis repo (not a diff against a prior
commit). Cover server (server/index.cjs, shared/backend-core.mjs, netlify/functions/
backend.mjs), daemon/agent CLI (agent/agensis-cli), database schema (database/
neon-schema.sql), MCP tooling (server/mcp.cjs), and the frontend (src/).

Goal: surface real defects and architectural risk — correctness bugs, security
issues (auth/authorization gaps, injection, secret handling), data-integrity risk
(schema/migration drift across the three backend mirrors), and structural problems
(inconsistency between server/index.cjs, shared/backend-core.mjs, and netlify/
functions/backend.mjs; dead/duplicated logic) — not style nitpicks.

Depth requested: stop after PLAN. Produce cascade/10-findings.cf (Stage F) and
cascade/20-plan.cf (Stage O) with grounded, citation-backed fix specs. Do NOT
execute Stage S — no code should be edited as part of this run. The plan is the
deliverable; a human decides what to apply and how.

ANSWER Q1: netlify/functions/backend.mjs is live in production. Treat F2
(forgeable auth secret fallback) and F4/F5 (triple-backend drift) as live,
exploitable, full priority — not legacy/dead code.
