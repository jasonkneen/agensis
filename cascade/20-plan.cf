HUMAN: Plan: forward radius/border tokens, one shared alias-driven contract, retheme both applets, verify in-browser.
PLAN P1<-F1 op=extend at=src/components/canvas/CanvasObjectRenderer.tsx:444-478 why=forward-tokens add tokens radiusSm/Md/Lg/Xl=token(--radius-*), borderWidth=(family==neo?2px:1px), neoStyle=attr(data-neo-style), success/warning/error; map radius names in APPLET_THEME_TOKEN_NAMES so canonical+alias both emitted order=1
PLAN P2<-F5 op=add at=src/lib/canvasApps.ts:70 why=shared-contract AGENSIS_APPLET_BASE_CSS(:root --app-* = var(--agensis-<kebab>, literal); universal control radius/shadow/border-width) + AGENSIS_APPLET_THEME_JS(generic applyAgensisTheme: body dataset family/scheme/neoStyle + setProperty --agensis-<kebab> per token) order=2
PLAN P3<-F4 op=rewrite at=src/lib/canvasApps.ts:79-300 why=kanban-consume include base css; controls use var(--app-*); drop neo radius hack; keep neo press-translate+accent-header gated on family; applyTheme=shared order=3
PLAN P4<-F3 op=rewrite at=src/lib/canvasApps.ts:386-571 why=calc-consume include base css; :root palette derives from --agensis-*; buttons use var(--radius-*); applyTheme=shared order=4
PLAN P5 op=verify why=visual render kanban static-injected classic vs neo-brutal(sharp) + live-switch, screenshot, assert radius/shadow/border differ; tsc+build order=5
DONE stage=O plan=5
