HUMAN: Themes analysed — 1 real bug (TW accent), brutal-dark shadow uneven, TW-dark ramp collapse.
FIND F1 sev=H tag=bug at=src/hooks/useTheme.ts:46-49#"syncNeoTheme();" what="scheme toggle re-runs applyTheme; syncNormalTheme() else-branch clearNormalTheme() strips --primary/--ring/--sh-accent that syncNeoTheme->applyThemePreset just set; App.tsx:606 only re-asserts on mount([] deps) so accent preset silently drops on light<->dark toggle for classic AND tinyworld" fix=re-assert-preset-after-syncNormalTheme ref=src/showcase/normalThemes.ts:32-46,src/App.tsx:603-609 dep=-
FIND F2 sev=H tag=feature at=src/index.css:409-437#"tinyworld light/dark blocks" what="TW hardcodes gold for --accent/--accent-hover/--accent-subtle/--accent-border/--ring and --tw-accent-*/--tw-control-ring; accent-preset picker IS shown for TW (SettingsDialog:501 !isNormalFamily) but only mutates --primary/--ring/--sh-accent so a picked hue never reaches TW accent surfaces -> #3 'influence TW by other themes' is unwired" fix=derive-tw-accent-tokens-from-var(--primary);keep--tw-gold-*fixed-as-brand-flourish dep=F1
FIND F3 sev=M tag=aesthetic at=src/showcase/neoThemes.ts:322#"const shadow = seed.shadow" what="~14 Pop/Pastel dark themes set no dark shadow seed; derived mix(ink,22,paper 78%) too dark on near-black paper so brutal hard-offset barely reads vs ~24 groups that hand-tune shadow:#5f-#6e" fix=lift-derived-dark-mix-22->34 note=do-NOT-reintroduce-translucent-black(reverted,~1.1:1) dep=-
FIND F4 sev=L tag=contrast at=src/index.css:226#"--muted-foreground: #c8bba7" what="TW dark --muted-foreground equals --text-secondary(#c8bba7) so the secondary/muted/text-muted emphasis ramp collapses to 2 steps" fix=set-muted-foreground~#a89c88 dep=-
FIND F5 sev=L tag=review at=src/showcase/normalThemes.ts:56#"const textMuted" what="expandNormal --text-muted=mix(fgMuted,55,bg): on already-dim dark seeds (solarized/nord) drops <3:1; acceptable-decorative but only systemic normal-dark contrast note; palettes otherwise sound" fix=none dep=-
OPEN Q1 scope="#3 = accent presets flow through (impl now) vs full named TW palette registry?" default=presets-flow-through
DONE stage=F find=5 open=1 tok~=430
