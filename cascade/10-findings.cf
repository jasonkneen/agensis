HUMAN: Applets under-themed — controls ignore radius/shadow/border-width; contract is per-applet and drifts.
FIND F1 sev=H tag=contract at=src/components/canvas/CanvasObjectRenderer.tsx:444-478#"function readAppletTheme" what="forwards colors+shadow but NOT radius/border-width/neo-style; applets cannot match brutal(square,hard-shadow,thick) vs soft(rounded)" fix=forward-radius+borderWidth+neoStyle dep=-
FIND F2 sev=H tag=drift at=src/lib/canvasApps.ts:285-300#"function applyTheme" what="two theme paths (static srcDoc inject + live postMessage applyTheme) hand-map different var names; live switch regresses" fix=both-paths-write-same --agensis-* aliases; applet CSS reads aliases only dep=F1
FIND F3 sev=M tag=inconsistent at=src/lib/canvasApps.ts:557-571#"function applyTheme" what="calculator applyTheme ignores family/radius entirely; stays fixed purple in every theme" fix=adopt shared contract dep=F2
FIND F4 sev=M tag=hardcoded at=src/lib/canvasApps.ts:121-198#"input, select, textarea" what="kanban controls hardcode 6px/4px radius + fixed box-shadow + neo radius hack; not token-driven" fix=consume var(--app-*) semantic vars dep=F2
FIND F5 sev=M tag=process at=src/lib/canvasApps.ts:1-70 what="no shared themeable base; each buildHtml reinvents theming => new applets ship unthemed" fix=shared AGENSIS_APPLET_BASE_CSS + AGENSIS_APPLET_THEME_JS constants both buildHtml include dep=F2
DONE stage=F find=5 open=0
